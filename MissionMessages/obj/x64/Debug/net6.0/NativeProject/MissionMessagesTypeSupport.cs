// <autogenerated>
// WARNING: THIS FILE IS AUTO-GENERATED. DO NOT MODIFY.
// DDS version: 3.25.0
// ACE version: 7.1.1
// Running on input file: C:\Projects\LearnDDS\MissionMessages/IDL\MissionMessages.idl
// </autogenerated>

using System;
using System.Linq;
using System.Security;
using System.Collections.Generic;
using System.Runtime.InteropServices;
using System.Text.Json;
using System.Text.Json.Serialization;
using OpenDDSharp.Marshaller.Json;
using OpenDDSharp;
using OpenDDSharp.DDS;
using OpenDDSharp.Marshaller;

namespace MissionModule
{
    #region Mission Definitions
    public class Mission
    {
        #region Constants
        internal const string API_DLL = "MissionMessagesWrapper";
        #endregion

        #region Fields
        Int32 _Key;
        string _Name;
        string _Description;
        string _Status;
        #endregion

        #region Properties
        public Int32 Key
        {
            get { return _Key; }
            set { _Key = value; }
        }

        public string Name
        {
            get { return _Name; }
            set { _Name = value; }
        }

        public string Description
        {
            get { return _Description; }
            set { _Description = value; }
        }

        public string Status
        {
            get { return _Status; }
            set { _Status = value; }
        }
        #endregion 

        #region Constructors
        public Mission()
        {
            _Key = 0;
            _Name = string.Empty;
            _Description = string.Empty;
            _Status = string.Empty;
        }
        #endregion

        #region Methods
        internal void MemberwiseCopy(Mission source)
        {
            Key = source.Key;
            Name = source.Name;
            Description = source.Description;
            Status = source.Status;
        }

        #endregion
    }

	public class MissionTypeSupport
    {
        #region Field
        private IntPtr _native;
        #endregion

        #region Constructors
        public MissionTypeSupport()
        {
            _native = MissionTypeSupportNew();
        }
        #endregion

        #region Methods
        public string GetTypeName()
        {
            return Marshal.PtrToStringAnsi(GetTypeName(_native));
        }

        public ReturnCode RegisterType(DomainParticipant dp, string typeName)
        {
            return (ReturnCode)RegisterType(_native, dp.ToNative(), typeName);
        }

        public ReturnCode UnregisterType(DomainParticipant dp, string typeName)
        {            
            return (ReturnCode)UnregisterType(_native, dp.ToNative(), typeName);
        }
        
        public static string EncodeToString(Mission sample)
        {
            return JsonSerializer.Serialize(sample, new JsonSerializerOptions
            {
                AllowTrailingCommas = false,
                Converters =
                {
                    new JsonStringEnumConverter(),
                    new OctetArrayConverter(),
                    new FloatJsonConverter(),
                    new DoubleJsonConverter(),
                    new DecimalJsonConverter(),
                }
            });
        }
        
        public static Mission DecodeFromString(string str)
        {
            return JsonSerializer.Deserialize<Mission>(str, new JsonSerializerOptions
            {
                AllowTrailingCommas = true,
                Converters =
                {
                    new JsonStringEnumConverter(),
                    new OctetArrayConverter(),
                    new FloatJsonConverter(),
                    new DoubleJsonConverter(),
                    new DecimalJsonConverter(),
                }
            });
        }
        #endregion

        #region PInvoke
        [SuppressUnmanagedCodeSecurity]
        [DllImport(Mission.API_DLL, EntryPoint = "MissionModule_MissionTypeSupport_new", CallingConvention = CallingConvention.Cdecl)]
        private static extern IntPtr MissionTypeSupportNew();

        [SuppressUnmanagedCodeSecurity]
        [DllImport(Mission.API_DLL, EntryPoint = "MissionModule_MissionTypeSupport_GetTypeName", CallingConvention = CallingConvention.Cdecl, CharSet = CharSet.Auto, BestFitMapping = false, ThrowOnUnmappableChar = true)]
        private static extern IntPtr GetTypeName(IntPtr native);

        [SuppressUnmanagedCodeSecurity]
        [DllImport(Mission.API_DLL, EntryPoint = "MissionModule_MissionTypeSupport_RegisterType", CallingConvention = CallingConvention.Cdecl, CharSet = CharSet.Ansi, BestFitMapping = false, ThrowOnUnmappableChar = true)]
        private static extern int RegisterType(IntPtr native, IntPtr dp, string typeName);

        [SuppressUnmanagedCodeSecurity]
        [DllImport(Mission.API_DLL, EntryPoint = "MissionModule_MissionTypeSupport_UnregisterType", CallingConvention = CallingConvention.Cdecl, CharSet = CharSet.Ansi, BestFitMapping = false, ThrowOnUnmappableChar = true)]
        private static extern int UnregisterType(IntPtr native, IntPtr dp, string typeName);
        #endregion
    }

    public class MissionDataWriter : DataWriter
    {
        #region Fields
        private readonly IntPtr _native;
        #endregion

        #region Constructors
        public MissionDataWriter(DataWriter dw) : base(dw.ToNative())
        {
			IntPtr ptr = base.ToNative();
            _native = Narrow(ptr);
        }
        #endregion

        #region Methods
        public InstanceHandle RegisterInstance(Mission instance)
        {
            InstanceHandle ret = InstanceHandle.HandleNil;

            var str = MissionTypeSupport.EncodeToString(instance);
            var json_data = MarshalHelper.NativeUtf8FromString(str);

            ret = RegisterInstance(_native, json_data);

            return ret;
        }

        public InstanceHandle RegisterInstance(Mission instance, Timestamp timestamp)
        {
            InstanceHandle ret = InstanceHandle.HandleNil;

            var str = MissionTypeSupport.EncodeToString(instance);
            var json_data = MarshalHelper.NativeUtf8FromString(str);

            ret = RegisterInstanceTimestamp(_native, json_data, timestamp);

            return ret;
        }

        public ReturnCode UnregisterInstance(Mission data)
        {
            InstanceHandle handle = LookupInstance(data);
            if (handle == InstanceHandle.HandleNil) 
            {
                return ReturnCode.PreconditionNotMet;
            }

            var str = MissionTypeSupport.EncodeToString(data);
            var json_data = MarshalHelper.NativeUtf8FromString(str);

	        return (ReturnCode)UnregisterInstance(_native, json_data, handle);
        }

		public ReturnCode UnregisterInstance(Mission data, InstanceHandle handle)
        {
            var str = MissionTypeSupport.EncodeToString(data);
            var json_data = MarshalHelper.NativeUtf8FromString(str);

            return (ReturnCode)UnregisterInstance(_native, json_data, handle);
        }

        public ReturnCode UnregisterInstance(Mission data, InstanceHandle handle, Timestamp timestamp)
        {
            var str = MissionTypeSupport.EncodeToString(data);
            var json_data = MarshalHelper.NativeUtf8FromString(str);

            return (ReturnCode)UnregisterInstanceTimestamp(_native, json_data, handle, timestamp);
        }

        public ReturnCode Write(Mission data)
        {
            return Write(data, InstanceHandle.HandleNil);
        }

        public ReturnCode Write(Mission data, InstanceHandle handle)
        {
            if (data == null)
            {
                return ReturnCode.BadParameter;
            }

            ReturnCode ret = ReturnCode.Error;

            var str = MissionTypeSupport.EncodeToString(data);
            var json_data = MarshalHelper.NativeUtf8FromString(str);

            ret = (ReturnCode)Write(_native, json_data, handle);

            return ret;
        }

        public ReturnCode Write(Mission data, InstanceHandle handle, Timestamp timestamp)
        {
            if (data == null)
            {
                return ReturnCode.BadParameter;
            }

            ReturnCode ret = ReturnCode.Error;

            var str = MissionTypeSupport.EncodeToString(data);
            var json_data = MarshalHelper.NativeUtf8FromString(str);

            ret = (ReturnCode)WriteWithTimestamp(_native, json_data, handle, timestamp);

            return ret;
        }

        public ReturnCode Dispose(Mission data)
        {
            return Dispose(data, InstanceHandle.HandleNil);
        }

		public ReturnCode Dispose(Mission data, InstanceHandle handle)
        {
            if (data == null)
            {
                return ReturnCode.BadParameter;
            }

            ReturnCode ret = ReturnCode.Error;

            var str = MissionTypeSupport.EncodeToString(data);
            var json_data = MarshalHelper.NativeUtf8FromString(str);

            ret = (ReturnCode)Dispose(_native, json_data, handle);
            
            return ret;
        }

        public ReturnCode Dispose(Mission data, InstanceHandle handle, Timestamp timestamp)
        {
            if (data == null)
            {
                return ReturnCode.BadParameter;
            }

            ReturnCode ret = ReturnCode.Error;

            var str = MissionTypeSupport.EncodeToString(data);
            var json_data = MarshalHelper.NativeUtf8FromString(str);

            ret = (ReturnCode)DisposeTimestamp(_native, json_data, handle, timestamp);

            return ret;
        }

        public ReturnCode GetKeyValue(Mission data, InstanceHandle handle)
        {
            if (data == null)
            {
                return ReturnCode.BadParameter;
            }

            if (handle == InstanceHandle.HandleNil)
            {
                return ReturnCode.BadParameter;
            }

            ReturnCode ret = ReturnCode.Error;
            
            var str = MissionTypeSupport.EncodeToString(data);
            var ptr = MarshalHelper.NativeUtf8FromString(str);
            ret = (ReturnCode)GetKeyValue(_native, ref ptr, handle);

            if (ret == ReturnCode.Ok)
            {
                var json_data = MarshalHelper.StringFromNativeUtf8(ptr) ?? string.Empty;

                if (!string.IsNullOrWhiteSpace(json_data))
                {
                    var sample = MissionTypeSupport.DecodeFromString(json_data);

                    data.MemberwiseCopy(sample);
                }

                MarshalHelper.ReleaseNativeStringPointer(ptr);
            }

            return ret;
        }

        public InstanceHandle LookupInstance(Mission instance)
        {
            InstanceHandle ret = InstanceHandle.HandleNil;
                        
            var str = MissionTypeSupport.EncodeToString(instance);
            var json_data = MarshalHelper.NativeUtf8FromString(str);

            ret = LookupInstance(_native, json_data);

            return ret;
        }
        #endregion

        #region PInvoke
        [SuppressUnmanagedCodeSecurity]
        [DllImport(Mission.API_DLL, EntryPoint = "MissionModule_MissionDataWriter_Narrow", CallingConvention = CallingConvention.Cdecl)]
        private static extern IntPtr Narrow(IntPtr dw);
        
        [SuppressUnmanagedCodeSecurity]
        [DllImport(Mission.API_DLL, EntryPoint = "MissionModule_MissionDataWriter_Write_Json", CallingConvention = CallingConvention.Cdecl, CharSet = CharSet.Ansi, BestFitMapping = false, ThrowOnUnmappableChar = true)]
        private static extern int Write(IntPtr dw, [In] IntPtr jsonData, int handle);

        [SuppressUnmanagedCodeSecurity]
        [DllImport(Mission.API_DLL, EntryPoint = "MissionModule_MissionDataWriter_WriteWithTimestamp_Json", CallingConvention = CallingConvention.Cdecl)]
        private static extern int WriteWithTimestamp(IntPtr dw, [In] IntPtr jsonData, int handle, [MarshalAs(UnmanagedType.Struct), In] Timestamp timestamp);

        [SuppressUnmanagedCodeSecurity]
        [DllImport(Mission.API_DLL, EntryPoint = "MissionModule_MissionDataWriter_RegisterInstance_Json", CallingConvention = CallingConvention.Cdecl)]
        private static extern int RegisterInstance(IntPtr dw, [In] IntPtr jsonData);

        [SuppressUnmanagedCodeSecurity]
        [DllImport(Mission.API_DLL, EntryPoint = "MissionModule_MissionDataWriter_RegisterInstanceTimestamp_Json", CallingConvention = CallingConvention.Cdecl)]
        private static extern int RegisterInstanceTimestamp(IntPtr dw, [In] IntPtr jsonData, [MarshalAs(UnmanagedType.Struct), In] Timestamp timestamp);

        [SuppressUnmanagedCodeSecurity]
        [DllImport(Mission.API_DLL, EntryPoint = "MissionModule_MissionDataWriter_UnregisterInstance_Json", CallingConvention = CallingConvention.Cdecl)]
        private static extern int UnregisterInstance(IntPtr dw, [In] IntPtr jsonData, int handle);

        [SuppressUnmanagedCodeSecurity]
        [DllImport(Mission.API_DLL, EntryPoint = "MissionModule_MissionDataWriter_UnregisterInstanceTimestamp_Json", CallingConvention = CallingConvention.Cdecl)]
        private static extern int UnregisterInstanceTimestamp(IntPtr dw, [In] IntPtr jsonData, int handle, [MarshalAs(UnmanagedType.Struct), In] Timestamp timestamp);

        [SuppressUnmanagedCodeSecurity]
        [DllImport(Mission.API_DLL, EntryPoint = "MissionModule_MissionDataWriter_LookupInstance_Json", CallingConvention = CallingConvention.Cdecl)]
        private static extern int LookupInstance(IntPtr dw, [In] IntPtr jsonData);

        [SuppressUnmanagedCodeSecurity]
        [DllImport(Mission.API_DLL, EntryPoint = "MissionModule_MissionDataWriter_Dispose_Json", CallingConvention = CallingConvention.Cdecl)]
        private static extern int Dispose(IntPtr dw, [In] IntPtr jsonData, int handle);

        [SuppressUnmanagedCodeSecurity]
        [DllImport(Mission.API_DLL, EntryPoint = "MissionModule_MissionDataWriter_DisposeTimestamp_Json", CallingConvention = CallingConvention.Cdecl)]
        private static extern int DisposeTimestamp(IntPtr dw, [In] IntPtr jsonData, int handle, [MarshalAs(UnmanagedType.Struct), In] Timestamp timestamp);

        [SuppressUnmanagedCodeSecurity]
        [DllImport(Mission.API_DLL, EntryPoint = "MissionModule_MissionDataWriter_GetKeyValue_Json", CallingConvention = CallingConvention.Cdecl)]
        private static extern int GetKeyValue(IntPtr dw, [In, Out] ref IntPtr json_data, int handle);
        #endregion
    }

    public class MissionDataReader : DataReader
    {
        #region Fields
        private readonly IntPtr _native;
        #endregion

        #region Constructors
        public MissionDataReader(DataReader dr) : base(dr.ToNative())
        {
			IntPtr ptr = base.ToNative();
            _native = Narrow(ptr);
        }
        #endregion

        #region Methods
        public ReturnCode Read(List<Mission> receivedData, List<SampleInfo> receivedInfo)
        {
            return Read(receivedData, receivedInfo, ResourceLimitsQosPolicy.LengthUnlimited, SampleStateMask.AnySampleState, ViewStateMask.AnyViewState, InstanceStateMask.AnyInstanceState);
        }

        public ReturnCode Read(List<Mission> receivedData, List<SampleInfo> receivedInfo, int maxSamples)
        {
            return Read(receivedData, receivedInfo, maxSamples, SampleStateMask.AnySampleState, ViewStateMask.AnyViewState, InstanceStateMask.AnyInstanceState);
        }

        public ReturnCode Read(List<Mission> receivedData, List<SampleInfo> receivedInfo, int maxSamples, ReadCondition condition)
        {
            if (condition == null)
            {
                return ReturnCode.BadParameter;
            }

            if (receivedData == null || receivedInfo == null)
            {
                return ReturnCode.BadParameter;
            }

            receivedData.Clear();
            receivedInfo.Clear();

            IntPtr rd = IntPtr.Zero;
            IntPtr ri = IntPtr.Zero;

            ReturnCode ret = ReturnCode.Error;
            ret = (ReturnCode)ReadWithCondition(_native, ref rd, ref ri, maxSamples, condition.ToNative());
            
            if (ret == ReturnCode.Ok && !rd.Equals(IntPtr.Zero) && !ri.Equals(IntPtr.Zero))
            {
                IList<string> data = new List<string>();
                IList<SampleInfoWrapper> info = new List<SampleInfoWrapper>();

                MarshalHelper.PtrToUTF8StringSequence(rd, ref data);
                MarshalHelper.PtrToSequence(ri, ref info);

                if (data != null)
                {
                    foreach (var d in data)
                    {                      
                        receivedData.Add(MissionTypeSupport.DecodeFromString(d));
                    }
                }

                if (info != null && info.Count > 0)
                {
                    foreach (var i in info)
                    {
                        SampleInfo aux = new SampleInfo();
                        aux.FromNative(i);
                        receivedInfo.Add(aux);
                    }
                }
                
                MarshalHelper.ReleaseNativeStringSequence(rd);
                MarshalHelper.ReleaseNativePointer(ri);
            }

            return ret;
        }

        public ReturnCode Read(List<Mission> receivedData, List<SampleInfo> receivedInfo, int maxSamples, SampleStateMask sampleStates, ViewStateMask viewStates, InstanceStateMask instanceStates)
        {
            if (receivedData == null || receivedInfo == null)
            {
                return ReturnCode.BadParameter;
            }

            receivedData.Clear();
            receivedInfo.Clear();

            IntPtr rd = IntPtr.Zero;
            IntPtr ri = IntPtr.Zero;

            ReturnCode ret = ReturnCode.Error;
            ret = (ReturnCode)Read(_native, ref rd, ref ri, maxSamples, sampleStates, viewStates, instanceStates);            

            if (ret == ReturnCode.Ok && !rd.Equals(IntPtr.Zero) && !ri.Equals(IntPtr.Zero))
            {
                IList<string> data = new List<string>();
                IList<SampleInfoWrapper> info = new List<SampleInfoWrapper>();

                MarshalHelper.PtrToUTF8StringSequence(rd, ref data);
                MarshalHelper.PtrToSequence(ri, ref info);

                if (data != null)
                {
                    foreach (var d in data)
                    {                      
                        receivedData.Add(MissionTypeSupport.DecodeFromString(d));
                    }
                }

                if (info != null && info.Count > 0)
                {
                    foreach (var i in info)
                    {
                        SampleInfo aux = new SampleInfo();
                        aux.FromNative(i);
                        receivedInfo.Add(aux);
                    }
                }
                
                MarshalHelper.ReleaseNativeStringSequence(rd);
                MarshalHelper.ReleaseNativePointer(ri);
            }

            return ret;
        }

        public ReturnCode Take(List<Mission> receivedData, List<SampleInfo> receivedInfo)
        {
            return Take(receivedData, receivedInfo, ResourceLimitsQosPolicy.LengthUnlimited, SampleStateMask.AnySampleState, ViewStateMask.AnyViewState, InstanceStateMask.AnyInstanceState);
        }

        public ReturnCode Take(List<Mission> receivedData, List<SampleInfo> receivedInfo, int maxSamples)
        {
            return Take(receivedData, receivedInfo, maxSamples, SampleStateMask.AnySampleState, ViewStateMask.AnyViewState, InstanceStateMask.AnyInstanceState);
        }

        public ReturnCode Take(List<Mission> receivedData, List<SampleInfo> receivedInfo, int maxSamples, ReadCondition condition)
        {
            if (condition == null)
            {
                return ReturnCode.BadParameter;
            }

            if (receivedData == null || receivedInfo == null)
            {
                return ReturnCode.BadParameter;
            }

            receivedData.Clear();
            receivedInfo.Clear();

            IntPtr rd = IntPtr.Zero;
            IntPtr ri = IntPtr.Zero;

            ReturnCode ret = ReturnCode.Error;
            ret = (ReturnCode)TakeWithCondition(_native, ref rd, ref ri, maxSamples, condition.ToNative());            

            if (ret == ReturnCode.Ok && !rd.Equals(IntPtr.Zero) && !ri.Equals(IntPtr.Zero))
            {
                IList<string> data = new List<string>();
                IList<SampleInfoWrapper> info = new List<SampleInfoWrapper>();

                MarshalHelper.PtrToUTF8StringSequence(rd, ref data);
                MarshalHelper.PtrToSequence(ri, ref info);

                if (data != null)
                {
                    foreach (var d in data)
                    {                      
                        receivedData.Add(MissionTypeSupport.DecodeFromString(d));
                    }
                }

                if (info != null && info.Count > 0)
                {
                    foreach (var i in info)
                    {
                        SampleInfo aux = new SampleInfo();
                        aux.FromNative(i);
                        receivedInfo.Add(aux);
                    }
                }
                
                MarshalHelper.ReleaseNativeStringSequence(rd);
                MarshalHelper.ReleaseNativePointer(ri);
            }

            return ret;
        }

        public ReturnCode Take(List<Mission> receivedData, List<SampleInfo> receivedInfo, int maxSamples, SampleStateMask sampleStates, ViewStateMask viewStates, InstanceStateMask instanceStates)
        {
            if (receivedData == null || receivedInfo == null)
            {
                return ReturnCode.BadParameter;
            }

            receivedData.Clear();
            receivedInfo.Clear();

            IntPtr rd = IntPtr.Zero;
            IntPtr ri = IntPtr.Zero;

            ReturnCode ret = ReturnCode.Error;
            ret = (ReturnCode)Take(_native, ref rd, ref ri, maxSamples, sampleStates, viewStates, instanceStates);

            if (ret == ReturnCode.Ok && !rd.Equals(IntPtr.Zero) && !ri.Equals(IntPtr.Zero))
            {
                IList<string> data = new List<string>();
                IList<SampleInfoWrapper> info = new List<SampleInfoWrapper>();

                MarshalHelper.PtrToUTF8StringSequence(rd, ref data);
                MarshalHelper.PtrToSequence(ri, ref info);

                if (data != null)
                {
                    foreach (var d in data)
                    {                      
                        receivedData.Add(MissionTypeSupport.DecodeFromString(d));
                    }
                }

                if (info != null && info.Count > 0)
                {
                    foreach (var i in info)
                    {
                        SampleInfo aux = new SampleInfo();
                        aux.FromNative(i);
                        receivedInfo.Add(aux);
                    }
                }
                
                MarshalHelper.ReleaseNativeStringSequence(rd);
                MarshalHelper.ReleaseNativePointer(ri);
            }

            return ret;
        }

        public ReturnCode ReadInstance(List<Mission> receivedData, List<SampleInfo> receivedInfo, InstanceHandle handle)
        {
            return ReadInstance(receivedData, receivedInfo, handle, ResourceLimitsQosPolicy.LengthUnlimited, SampleStateMask.AnySampleState, ViewStateMask.AnyViewState, InstanceStateMask.AnyInstanceState);
        }

		public ReturnCode ReadInstance(List<Mission> receivedData, List<SampleInfo> receivedInfo, InstanceHandle handle, int maxSamples)
        {
            return ReadInstance(receivedData, receivedInfo, handle, maxSamples, SampleStateMask.AnySampleState, ViewStateMask.AnyViewState, InstanceStateMask.AnyInstanceState);
        }

		public ReturnCode ReadInstance(List<Mission> receivedData, List<SampleInfo> receivedInfo, InstanceHandle handle, int maxSamples, ReadCondition condition)
        {
            if (condition == null)
            {
                return ReturnCode.BadParameter;
            }

            if (receivedData == null || receivedInfo == null)
            {
                return ReturnCode.BadParameter;
            }

            receivedData.Clear();
            receivedInfo.Clear();

            IntPtr rd = IntPtr.Zero;
            IntPtr ri = IntPtr.Zero;

            ReturnCode ret = ReturnCode.Error;
            ret = (ReturnCode)ReadInstanceWithCondition(_native, ref rd, ref ri, (int)handle, maxSamples, condition.ToNative());

            if (ret == ReturnCode.Ok && !rd.Equals(IntPtr.Zero) && !ri.Equals(IntPtr.Zero))
            {
                IList<string> data = new List<string>();
                IList<SampleInfoWrapper> info = new List<SampleInfoWrapper>();

                MarshalHelper.PtrToUTF8StringSequence(rd, ref data);
                MarshalHelper.PtrToSequence(ri, ref info);

                if (data != null)
                {
                    foreach (var d in data)
                    {                      
                        receivedData.Add(MissionTypeSupport.DecodeFromString(d));
                    }
                }

                if (info != null && info.Count > 0)
                {
                    foreach (var i in info)
                    {
                        SampleInfo aux = new SampleInfo();
                        aux.FromNative(i);
                        receivedInfo.Add(aux);
                    }
                }
                
                MarshalHelper.ReleaseNativeStringSequence(rd);
                MarshalHelper.ReleaseNativePointer(ri);
            }

            return ret;
        }

        public ReturnCode ReadInstance(List<Mission> receivedData, List<SampleInfo> receivedInfo, InstanceHandle handle, int maxSamples, SampleStateMask sampleStates, ViewStateMask viewStates, InstanceStateMask instanceStates)
        {
            if (receivedData == null || receivedInfo == null)
            {
                return ReturnCode.BadParameter;
            }

            receivedData.Clear();
            receivedInfo.Clear();

            IntPtr rd = IntPtr.Zero;
            IntPtr ri = IntPtr.Zero;

            ReturnCode ret = ReturnCode.Error;
            ret = (ReturnCode)ReadInstance(_native, ref rd, ref ri, handle, maxSamples, sampleStates, viewStates, instanceStates);

            if (ret == ReturnCode.Ok && !rd.Equals(IntPtr.Zero) && !ri.Equals(IntPtr.Zero))
            {
                IList<string> data = new List<string>();
                IList<SampleInfoWrapper> info = new List<SampleInfoWrapper>();

                MarshalHelper.PtrToUTF8StringSequence(rd, ref data);
                MarshalHelper.PtrToSequence(ri, ref info);

                if (data != null)
                {
                    foreach (var d in data)
                    {                      
                        receivedData.Add(MissionTypeSupport.DecodeFromString(d));
                    }
                }

                if (info != null && info.Count > 0)
                {
                    foreach (var i in info)
                    {
                        SampleInfo aux = new SampleInfo();
                        aux.FromNative(i);
                        receivedInfo.Add(aux);
                    }
                }
                
                MarshalHelper.ReleaseNativeStringSequence(rd);
                MarshalHelper.ReleaseNativePointer(ri);
            }

            return ret;
        }

        public ReturnCode TakeInstance(List<Mission> receivedData, List<SampleInfo> receivedInfo, InstanceHandle handle)
        {
            return TakeInstance(receivedData, receivedInfo, handle, ResourceLimitsQosPolicy.LengthUnlimited, SampleStateMask.AnySampleState, ViewStateMask.AnyViewState, InstanceStateMask.AnyInstanceState);
        }

		public ReturnCode TakeInstance(List<Mission> receivedData, List<SampleInfo> receivedInfo, InstanceHandle handle, int maxSamples)
        {
            return TakeInstance(receivedData, receivedInfo, handle, maxSamples, SampleStateMask.AnySampleState, ViewStateMask.AnyViewState, InstanceStateMask.AnyInstanceState);
        }

		public ReturnCode TakeInstance(List<Mission> receivedData, List<SampleInfo> receivedInfo, InstanceHandle handle, int maxSamples, ReadCondition condition)
        {
            if (condition == null)
            {
                return ReturnCode.BadParameter;
            }

            if (receivedData == null || receivedInfo == null)
            {
                return ReturnCode.BadParameter;
            }

            receivedData.Clear();
            receivedInfo.Clear();

            IntPtr rd = IntPtr.Zero;
            IntPtr ri = IntPtr.Zero;

            ReturnCode ret = ReturnCode.Error;
            ret = (ReturnCode)TakeInstanceWithCondition(_native, ref rd, ref ri, (int)handle, maxSamples, condition.ToNative());

            if (ret == ReturnCode.Ok && !rd.Equals(IntPtr.Zero) && !ri.Equals(IntPtr.Zero))
            {
                IList<string> data = new List<string>();
                IList<SampleInfoWrapper> info = new List<SampleInfoWrapper>();

                MarshalHelper.PtrToUTF8StringSequence(rd, ref data);
                MarshalHelper.PtrToSequence(ri, ref info);

                if (data != null)
                {
                    foreach (var d in data)
                    {                      
                        receivedData.Add(MissionTypeSupport.DecodeFromString(d));
                    }
                }

                if (info != null && info.Count > 0)
                {
                    foreach (var i in info)
                    {
                        SampleInfo aux = new SampleInfo();
                        aux.FromNative(i);
                        receivedInfo.Add(aux);
                    }
                }
                
                MarshalHelper.ReleaseNativeStringSequence(rd);
                MarshalHelper.ReleaseNativePointer(ri);
            }

            return ret;
        }

        public ReturnCode TakeInstance(List<Mission> receivedData, List<SampleInfo> receivedInfo, InstanceHandle handle, int maxSamples, SampleStateMask sampleStates, ViewStateMask viewStates, InstanceStateMask instanceStates)
        {
            if (receivedData == null || receivedInfo == null)
            {
                return ReturnCode.BadParameter;
            }

            receivedData.Clear();
            receivedInfo.Clear();

            IntPtr rd = IntPtr.Zero;
            IntPtr ri = IntPtr.Zero;

            ReturnCode ret = ReturnCode.Error;
            ret = (ReturnCode)TakeInstance(_native, ref rd, ref ri, handle, maxSamples, sampleStates, viewStates, instanceStates);

            if (ret == ReturnCode.Ok && !rd.Equals(IntPtr.Zero) && !ri.Equals(IntPtr.Zero))
            {
                IList<string> data = new List<string>();
                IList<SampleInfoWrapper> info = new List<SampleInfoWrapper>();

                MarshalHelper.PtrToUTF8StringSequence(rd, ref data);
                MarshalHelper.PtrToSequence(ri, ref info);

                if (data != null)
                {
                    foreach (var d in data)
                    {                      
                        receivedData.Add(MissionTypeSupport.DecodeFromString(d));
                    }
                }

                if (info != null && info.Count > 0)
                {
                    foreach (var i in info)
                    {
                        SampleInfo aux = new SampleInfo();
                        aux.FromNative(i);
                        receivedInfo.Add(aux);
                    }
                }
                
                MarshalHelper.ReleaseNativeStringSequence(rd);
                MarshalHelper.ReleaseNativePointer(ri);
            }

            return ret;
        }

        public ReturnCode ReadNextInstance(List<Mission> receivedData, List<SampleInfo> receivedInfo, InstanceHandle previousHandle)
        {
            return ReadNextInstance(receivedData, receivedInfo, previousHandle, ResourceLimitsQosPolicy.LengthUnlimited, SampleStateMask.AnySampleState, ViewStateMask.AnyViewState, InstanceStateMask.AnyInstanceState);
        }

		public ReturnCode ReadNextInstance(List<Mission> receivedData, List<SampleInfo> receivedInfo, InstanceHandle previousHandle, int maxSamples)
        {
            return ReadNextInstance(receivedData, receivedInfo, previousHandle, maxSamples, SampleStateMask.AnySampleState, ViewStateMask.AnyViewState, InstanceStateMask.AnyInstanceState);
        }

		public ReturnCode ReadNextInstance(List<Mission> receivedData, List<SampleInfo> receivedInfo, InstanceHandle previousHandle, int maxSamples, ReadCondition condition)
        {
            if (condition == null)
            {
                return ReturnCode.BadParameter;
            }

            if (receivedData == null || receivedInfo == null)
            {
                return ReturnCode.BadParameter;
            }

            receivedData.Clear();
            receivedInfo.Clear();

            IntPtr rd = IntPtr.Zero;
            IntPtr ri = IntPtr.Zero;

            ReturnCode ret = ReturnCode.Error;
            ret = (ReturnCode)ReadNextInstanceWithCondition(_native, ref rd, ref ri, previousHandle, maxSamples, condition.ToNative());

            if (ret == ReturnCode.Ok && !rd.Equals(IntPtr.Zero) && !ri.Equals(IntPtr.Zero))
            {
                IList<string> data = new List<string>();
                IList<SampleInfoWrapper> info = new List<SampleInfoWrapper>();

                MarshalHelper.PtrToUTF8StringSequence(rd, ref data);
                MarshalHelper.PtrToSequence(ri, ref info);

                if (data != null)
                {
                    foreach (var d in data)
                    {                      
                        receivedData.Add(MissionTypeSupport.DecodeFromString(d));
                    }
                }

                if (info != null && info.Count > 0)
                {
                    foreach (var i in info)
                    {
                        SampleInfo aux = new SampleInfo();
                        aux.FromNative(i);
                        receivedInfo.Add(aux);
                    }
                }
                
                MarshalHelper.ReleaseNativeStringSequence(rd);
                MarshalHelper.ReleaseNativePointer(ri);
            }

            return ret;
        }

        public ReturnCode ReadNextInstance(List<Mission> receivedData, List<SampleInfo> receivedInfo, InstanceHandle previousHandle, int maxSamples, SampleStateMask sampleStates, ViewStateMask viewStates, InstanceStateMask instanceStates)
        {
            if (receivedData == null || receivedInfo == null)
            {
                return ReturnCode.BadParameter;
            }

            receivedData.Clear();
            receivedInfo.Clear();

            IntPtr rd = IntPtr.Zero;
            IntPtr ri = IntPtr.Zero;

            ReturnCode ret = ReturnCode.Error;
            ret = (ReturnCode)ReadNextInstance(_native, ref rd, ref ri, previousHandle, maxSamples, sampleStates, viewStates, instanceStates);

            if (ret == ReturnCode.Ok && !rd.Equals(IntPtr.Zero) && !ri.Equals(IntPtr.Zero))
            {
                IList<string> data = new List<string>();
                IList<SampleInfoWrapper> info = new List<SampleInfoWrapper>();

                MarshalHelper.PtrToUTF8StringSequence(rd, ref data);
                MarshalHelper.PtrToSequence(ri, ref info);

                if (data != null)
                {
                    foreach (var d in data)
                    {                      
                        receivedData.Add(MissionTypeSupport.DecodeFromString(d));
                    }
                }

                if (info != null && info.Count > 0)
                {
                    foreach (var i in info)
                    {
                        SampleInfo aux = new SampleInfo();
                        aux.FromNative(i);
                        receivedInfo.Add(aux);
                    }
                }
                
                MarshalHelper.ReleaseNativeStringSequence(rd);
                MarshalHelper.ReleaseNativePointer(ri);
            }

            return ret;
        }

        public ReturnCode TakeNextInstance(List<Mission> receivedData, List<SampleInfo> receivedInfo, InstanceHandle previousHandle)
        {
            return TakeNextInstance(receivedData, receivedInfo, previousHandle, ResourceLimitsQosPolicy.LengthUnlimited, SampleStateMask.AnySampleState, ViewStateMask.AnyViewState, InstanceStateMask.AnyInstanceState);
        }

		public ReturnCode TakeNextInstance(List<Mission> receivedData, List<SampleInfo> receivedInfo, InstanceHandle previousHandle, int maxSamples)
        {
            return TakeNextInstance(receivedData, receivedInfo, previousHandle, maxSamples, SampleStateMask.AnySampleState, ViewStateMask.AnyViewState, InstanceStateMask.AnyInstanceState);
        }

		public ReturnCode TakeNextInstance(List<Mission> receivedData, List<SampleInfo> receivedInfo, InstanceHandle previousHandle, int maxSamples, ReadCondition condition)
        {
            if (condition == null)
            {
                return ReturnCode.BadParameter;
            }

            if (receivedData == null || receivedInfo == null)
            {
                return ReturnCode.BadParameter;
            }

            receivedData.Clear();
            receivedInfo.Clear();

            IntPtr rd = IntPtr.Zero;
            IntPtr ri = IntPtr.Zero;

            ReturnCode ret = ReturnCode.Error;
            ret = (ReturnCode)TakeNextInstanceWithCondition(_native, ref rd, ref ri, previousHandle, maxSamples, condition.ToNative());

            if (ret == ReturnCode.Ok && !rd.Equals(IntPtr.Zero) && !ri.Equals(IntPtr.Zero))
            {
                IList<string> data = new List<string>();
                IList<SampleInfoWrapper> info = new List<SampleInfoWrapper>();

                MarshalHelper.PtrToUTF8StringSequence(rd, ref data);
                MarshalHelper.PtrToSequence(ri, ref info);

                if (data != null)
                {
                    foreach (var d in data)
                    {                      
                        receivedData.Add(MissionTypeSupport.DecodeFromString(d));
                    }
                }

                if (info != null && info.Count > 0)
                {
                    foreach (var i in info)
                    {
                        SampleInfo aux = new SampleInfo();
                        aux.FromNative(i);
                        receivedInfo.Add(aux);
                    }
                }
                
                MarshalHelper.ReleaseNativeStringSequence(rd);
                MarshalHelper.ReleaseNativePointer(ri);
            }

            return ret;
        }

        public ReturnCode TakeNextInstance(List<Mission> receivedData, List<SampleInfo> receivedInfo, InstanceHandle previousHandle, int maxSamples, SampleStateMask sampleStates, ViewStateMask viewStates, InstanceStateMask instanceStates)
        {
            if (receivedData == null || receivedInfo == null)
            {
                return ReturnCode.BadParameter;
            }

            receivedData.Clear();
            receivedInfo.Clear();

            IntPtr rd = IntPtr.Zero;
            IntPtr ri = IntPtr.Zero;

            ReturnCode ret = ReturnCode.Error;
            ret = (ReturnCode)TakeNextInstance(_native, ref rd, ref ri, previousHandle, maxSamples, sampleStates, viewStates, instanceStates);

            if (ret == ReturnCode.Ok && !rd.Equals(IntPtr.Zero) && !ri.Equals(IntPtr.Zero))
            {
                IList<string> data = new List<string>();
                IList<SampleInfoWrapper> info = new List<SampleInfoWrapper>();

                MarshalHelper.PtrToUTF8StringSequence(rd, ref data);
                MarshalHelper.PtrToSequence(ri, ref info);

                if (data != null)
                {
                    foreach (var d in data)
                    {                      
                        receivedData.Add(MissionTypeSupport.DecodeFromString(d));
                    }
                }

                if (info != null && info.Count > 0)
                {
                    foreach (var i in info)
                    {
                        SampleInfo aux = new SampleInfo();
                        aux.FromNative(i);
                        receivedInfo.Add(aux);
                    }
                }
                
                MarshalHelper.ReleaseNativeStringSequence(rd);
                MarshalHelper.ReleaseNativePointer(ri);
            }

            return ret;
        }

        public ReturnCode ReadNextSample(Mission data, SampleInfo sampleInfo)
        {
            if (data == null || sampleInfo == null)
            {
                return ReturnCode.BadParameter;
            }

            ReturnCode ret = ReturnCode.Error;
            IntPtr ptr = IntPtr.Zero;
            SampleInfoWrapper infoWrapper = new SampleInfoWrapper();
            
            ret = (ReturnCode)ReadNextSample(_native, ref ptr, ref infoWrapper);

            if (ret == ReturnCode.Ok)
            {
                var json_data = MarshalHelper.StringFromNativeUtf8(ptr) ?? string.Empty;
                
                if (!string.IsNullOrWhiteSpace(json_data))
                {
                    var sample = MissionTypeSupport.DecodeFromString(json_data);

                    data.MemberwiseCopy(sample);
                }

                MarshalHelper.ReleaseNativeStringPointer(ptr);
                
                sampleInfo.FromNative(infoWrapper);
            }

            return ret;
        }

        public ReturnCode TakeNextSample(Mission data, SampleInfo sampleInfo)
        {
            if (data == null || sampleInfo == null)
            {
                return ReturnCode.BadParameter;
            }

            ReturnCode ret = ReturnCode.Error;
            var ptr = IntPtr.Zero;
            var infoWrapper = new SampleInfoWrapper();
            ret = (ReturnCode)TakeNextSample(_native, ref ptr, ref infoWrapper);
            if (ret == ReturnCode.Ok)
            {
                var json_data = MarshalHelper.StringFromNativeUtf8(ptr) ?? string.Empty;
            
                if (!string.IsNullOrWhiteSpace(json_data))
                {
                    var sample = MissionTypeSupport.DecodeFromString(json_data);

                    data.MemberwiseCopy(sample);
                }
                
                MarshalHelper.ReleaseNativeStringPointer(ptr);

                sampleInfo.FromNative(infoWrapper);
            }

            return ret;
        }

        public ReturnCode GetKeyValue(Mission data, InstanceHandle handle)
        {
            if (data == null)
            {
                return ReturnCode.BadParameter;
            }

            if (handle == InstanceHandle.HandleNil)
            {
                return ReturnCode.BadParameter;
            }

            var str = MissionTypeSupport.EncodeToString(data);
            var ptr = MarshalHelper.NativeUtf8FromString(str);

            var ret = (ReturnCode)GetKeyValue(_native, ref ptr, handle);            

            if (ret == ReturnCode.Ok)
            {
                var json_data = MarshalHelper.StringFromNativeUtf8(ptr) ?? string.Empty;
                
                if (!string.IsNullOrWhiteSpace(json_data))
                {
                    var sample = MissionTypeSupport.DecodeFromString(json_data);

                    data.MemberwiseCopy(sample);
                }

                MarshalHelper.ReleaseNativeStringPointer(ptr);
            }

            return ret;
        }

        public InstanceHandle LookupInstance(Mission instance)
        {
            InstanceHandle ret = InstanceHandle.HandleNil;

            var json_data = MissionTypeSupport.EncodeToString(instance);

            ret = LookupInstance(_native, json_data);

            return ret;
        }
        #endregion

        #region PInvoke
        [SuppressUnmanagedCodeSecurity]
        [DllImport(Mission.API_DLL, EntryPoint = "MissionModule_MissionDataReader_Narrow", CallingConvention = CallingConvention.Cdecl)]
        private static extern IntPtr Narrow(IntPtr dr);

        [SuppressUnmanagedCodeSecurity]
        [DllImport(Mission.API_DLL, EntryPoint = "MissionModule_MissionDataReader_Read_Json", CallingConvention = CallingConvention.Cdecl)]
        private static extern int Read(IntPtr dr, ref IntPtr receivedData, ref IntPtr receivedInfo, int maxSamples, uint sampleStates, uint viewStates, uint instanceStates);

        [SuppressUnmanagedCodeSecurity]
        [DllImport(Mission.API_DLL, EntryPoint = "MissionModule_MissionDataReader_ReadWithCondition_Json", CallingConvention = CallingConvention.Cdecl)]
        private static extern int ReadWithCondition(IntPtr dr, ref IntPtr receivedData, ref IntPtr receivedInfo, int maxSamples, IntPtr condition);

        [SuppressUnmanagedCodeSecurity]
        [DllImport(Mission.API_DLL, EntryPoint = "MissionModule_MissionDataReader_Take_Json", CallingConvention = CallingConvention.Cdecl)]
        private static extern int Take(IntPtr dr, ref IntPtr receivedData, ref IntPtr receivedInfo, int maxSamples, uint sampleStates, uint viewStates, uint instanceStates);

        [SuppressUnmanagedCodeSecurity]
        [DllImport(Mission.API_DLL, EntryPoint = "MissionModule_MissionDataReader_TakeWithCondition_Json", CallingConvention = CallingConvention.Cdecl)]
        private static extern int TakeWithCondition(IntPtr dr, ref IntPtr receivedData, ref IntPtr receivedInfo, int maxSamples, IntPtr condition);

        [SuppressUnmanagedCodeSecurity]
        [DllImport(Mission.API_DLL, EntryPoint = "MissionModule_MissionDataReader_ReadInstance_Json", CallingConvention = CallingConvention.Cdecl)]
        private static extern int ReadInstance(IntPtr dr, ref IntPtr receivedData, ref IntPtr receivedInfo, int handle, int maxSamples, uint sampleStates, uint viewStates, uint instanceStates);

        [SuppressUnmanagedCodeSecurity]
        [DllImport(Mission.API_DLL, EntryPoint = "MissionModule_MissionDataReader_ReadInstanceWithCondition_Json", CallingConvention = CallingConvention.Cdecl)]
        private static extern int ReadInstanceWithCondition(IntPtr dr, ref IntPtr receivedData, ref IntPtr receivedInfo, int handle, int maxSamples, IntPtr condition);

        [SuppressUnmanagedCodeSecurity]
        [DllImport(Mission.API_DLL, EntryPoint = "MissionModule_MissionDataReader_TakeInstance_Json", CallingConvention = CallingConvention.Cdecl)]
        private static extern int TakeInstance(IntPtr dr, ref IntPtr receivedData, ref IntPtr receivedInfo, int handle, int maxSamples, uint sampleStates, uint viewStates, uint instanceStates);

        [SuppressUnmanagedCodeSecurity]
        [DllImport(Mission.API_DLL, EntryPoint = "MissionModule_MissionDataReader_TakeInstanceWithCondition_Json", CallingConvention = CallingConvention.Cdecl)]
        private static extern int TakeInstanceWithCondition(IntPtr dr, ref IntPtr receivedData, ref IntPtr receivedInfo, int handle, int maxSamples, IntPtr condition);

        [SuppressUnmanagedCodeSecurity]
        [DllImport(Mission.API_DLL, EntryPoint = "MissionModule_MissionDataReader_ReadNextInstance_Json", CallingConvention = CallingConvention.Cdecl)]
        private static extern int ReadNextInstance(IntPtr dr, ref IntPtr receivedData, ref IntPtr receivedInfo, int handle, int maxSamples, uint sampleStates, uint viewStates, uint instanceStates);

        [SuppressUnmanagedCodeSecurity]
        [DllImport(Mission.API_DLL, EntryPoint = "MissionModule_MissionDataReader_ReadNextInstanceWithCondition_Json", CallingConvention = CallingConvention.Cdecl)]
        private static extern int ReadNextInstanceWithCondition(IntPtr dr, ref IntPtr receivedData, ref IntPtr receivedInfo, int handle, int maxSamples, IntPtr condition);

        [SuppressUnmanagedCodeSecurity]
        [DllImport(Mission.API_DLL, EntryPoint = "MissionModule_MissionDataReader_TakeNextInstance_Json", CallingConvention = CallingConvention.Cdecl)]
        private static extern int TakeNextInstance(IntPtr dr, ref IntPtr receivedData, ref IntPtr receivedInfo, int handle, int maxSamples, uint sampleStates, uint viewStates, uint instanceStates);

        [SuppressUnmanagedCodeSecurity]
        [DllImport(Mission.API_DLL, EntryPoint = "MissionModule_MissionDataReader_TakeNextInstanceWithCondition_Json", CallingConvention = CallingConvention.Cdecl)]
        private static extern int TakeNextInstanceWithCondition(IntPtr dr, ref IntPtr receivedData, ref IntPtr receivedInfo, int handle, int maxSamples, IntPtr condition);

        [SuppressUnmanagedCodeSecurity]
        [DllImport(Mission.API_DLL, EntryPoint = "MissionModule_MissionDataReader_ReadNextSample_Json", CallingConvention = CallingConvention.Cdecl)]
        private static extern int ReadNextSample(IntPtr dr, [In, Out] ref IntPtr json_data, [In, Out] ref SampleInfoWrapper sampleInfo);

        [SuppressUnmanagedCodeSecurity]
        [DllImport(Mission.API_DLL, EntryPoint = "MissionModule_MissionDataReader_TakeNextSample_Json", CallingConvention = CallingConvention.Cdecl)]
        private static extern int TakeNextSample(IntPtr dr, [In, Out] ref IntPtr json_data, [In, Out] ref SampleInfoWrapper sampleInfo);

        [SuppressUnmanagedCodeSecurity]
        [DllImport(Mission.API_DLL, EntryPoint = "MissionModule_MissionDataReader_LookupInstance_Json", CallingConvention = CallingConvention.Cdecl)]
        private static extern int LookupInstance(IntPtr dr, [MarshalAs(UnmanagedType.LPStr), In]string jsonData);

        [SuppressUnmanagedCodeSecurity]
        [DllImport(Mission.API_DLL, EntryPoint = "MissionModule_MissionDataReader_GetKeyValue_Json", CallingConvention = CallingConvention.Cdecl)]
        private static extern int GetKeyValue(IntPtr dr, [In, Out] ref IntPtr data, int handle);
        #endregion
    }
    #endregion

}
